#include <stdio.h>

int main () {
/*Below instruction will throw an error
 *error: declaration of ‘arr’ as array of voids
 *This singnifies, we can not declear an array of void
 */
    void arr[10];
    return 0;
}
